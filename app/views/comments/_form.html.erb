<%= form_for([@article, @article.comments.build]) do |f| %>
<!-- this form_for uses an array to build a nested route like: articles/1/comments .. its like for article 1, build the comments array which u access thru params[:comment] which gives u access to the :comment object which gives access to params[:commenter] and params[:body] the 2 properties of a comment object.-->
  <p>
    <%= f.label :commenter %>
    <%= f.text_field :commenter %>
  </p>

  <p>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit %>
    <!-- submitting this will send u to a Post for comments which sends u to the CREATE method in Comments controller. -->
  </p>

<% end %>